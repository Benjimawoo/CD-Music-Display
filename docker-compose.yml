services:
  cd-music-display:
    build: .
    container_name: cd-music-display
    restart: unless-stopped
    ports:
      - "${PORT:-443}:3000"
    volumes:
      - ./data:/app/data
      - ./certs:/app/certs:ro
    environment:
      - NODE_ENV=production
